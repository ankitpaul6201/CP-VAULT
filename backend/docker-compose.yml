version: '3.8'

services:
  oauth-proxy:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
    restart: always
